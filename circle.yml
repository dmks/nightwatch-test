## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezone

  node:
    version: 0.12.2

dependencies:
  override:

test:
  post:
    - npm install
    - npm run integration_tests

## Customize the test machine
machine:

  node:
    version: 0.12.2

dependencies:
  post:
    - npm install
    - npm run selenium_server:
        background: true
      
test:
  post:
    - npm run integration_tests_circleci

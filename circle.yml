## Customize the test machine
machine:

  node:
    version: 0.12.2

dependencies:
  post:
    - java -jar spec/nightwatch/lib/selenium-server-standalone-2.44.0.jar:
      background: true

test:
  post:
    - npm install
    - npm run integration_tests
